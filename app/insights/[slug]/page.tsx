import type { Metadata } from 'next';
import { notFound } from 'next/navigation';
import Link from 'next/link';
import { insights } from '@/app/lib/insights-content';
import { growthMetadata, SITE_URL } from '@/app/lib/growth-content';

export function generateStaticParams() { return insights.map(({ slug }) => ({ slug })); }

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> {
  const { slug } = await params;
  const insight = insights.find(item => item.slug === slug);
  if (!insight) return {};
  return growthMetadata(insight.title, insight.description, `/insights/${slug}`);
}

export default async function InsightPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const insight = insights.find(item => item.slug === slug);
  if (!insight) notFound();
  const path = `/insights/${insight.slug}`;
  const article = { '@context': 'https://schema.org', '@type': 'Article', headline: insight.title, description: insight.description, dateModified: '2026-07-22', datePublished: '2026-07-22', author: { '@type': 'Organization', name: 'OfRoot Technology' }, publisher: { '@type': 'Organization', name: 'OfRoot', url: SITE_URL }, mainEntityOfPage: `${SITE_URL}${path}` };
  const faq = { '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: insight.faq.map(item => ({ '@type': 'Question', name: item.question, acceptedAnswer: { '@type': 'Answer', text: item.answer } })) };
  const breadcrumb = { '@context': 'https://schema.org', '@type': 'BreadcrumbList', itemListElement: [{ '@type': 'ListItem', position: 1, name: 'Home', item: SITE_URL }, { '@type': 'ListItem', position: 2, name: 'Insights', item: `${SITE_URL}/insights` }, { '@type': 'ListItem', position: 3, name: insight.title, item: `${SITE_URL}${path}` }] };
  return <main id="main-content" className="bg-[#f7f6f2] text-slate-950"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(article) }} /><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faq) }} /><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(breadcrumb) }} /><article><header className="bg-[#071225] px-6 py-16 text-white sm:px-8 sm:py-24"><div className="mx-auto max-w-4xl"><nav aria-label="Breadcrumb" className="mb-9 text-sm text-slate-400"><Link href="/">Home</Link> / <Link href="/insights">Insights</Link> / <span className="text-white">{insight.category}</span></nav><p className="mb-4 text-sm font-bold uppercase tracking-[.18em] text-[#FFC46B]">{insight.category}</p><h1 className="text-balance text-4xl font-black text-white sm:text-6xl">{insight.title}</h1><div className="mt-7 flex flex-wrap gap-4 text-sm text-slate-300"><span>By OfRoot Technology</span><span>Updated {insight.updated}</span></div></div></header><div className="mx-auto grid max-w-6xl gap-12 px-6 py-16 sm:px-8 lg:grid-cols-[220px_1fr]"><aside><nav aria-label="Table of contents" className="sticky top-24 rounded-2xl border border-slate-200 bg-white p-5"><p className="mb-3 font-bold">On this page</p><ol className="space-y-2 text-sm text-slate-600"><li><a href="#answer">Direct answer</a></li>{insight.outline.map(section => <li key={section.title}><a href={`#${section.title.toLowerCase().replaceAll(' ', '-')}`}>{section.title}</a></li>)}<li><a href="#evidence">Evidence and sources</a></li><li><a href="#faq">FAQ</a></li></ol></nav></aside><div className="max-w-3xl"><section id="answer" className="rounded-3xl border border-[#FF9312]/30 bg-[#fff8ee] p-7"><p className="mb-3 text-xs font-bold uppercase tracking-[.15em] text-[#9A4A00]">Direct answer</p><p className="mx-0 text-xl font-semibold leading-relaxed">{insight.directAnswer}</p></section>{insight.outline.map((section, index) => <section key={section.title} id={section.title.toLowerCase().replaceAll(' ', '-')} className="border-b border-slate-200 py-12"><p className="mb-3 text-xs font-bold text-[#B55B00]">0{index + 1}</p><h2 className="text-3xl font-black">{section.title}</h2><ul className="mt-6 space-y-3 text-slate-700">{section.points.map(point => <li key={point} className="flex gap-3"><span aria-hidden="true" className="text-[#B55B00]">→</span><span>{point}</span></li>)}</ul></section>)}<section id="evidence" className="border-b border-slate-200 py-12"><h2 className="text-3xl font-black">Evidence and sources</h2><p className="mx-0 mt-4 text-slate-600">Use these primary references to validate implementation details as the outline becomes a full article.</p><ul className="mt-5 space-y-2">{insight.sources.map(source => <li key={source.href}><a href={source.href} target="_blank" rel="noopener noreferrer" className="font-semibold text-[#8F4700] hover:underline">{source.label} ↗</a></li>)}</ul></section><section id="faq" className="py-12"><h2 className="text-3xl font-black">Frequently asked questions</h2><div className="mt-6 space-y-4">{insight.faq.map(item => <details key={item.question} className="rounded-2xl border border-slate-200 bg-white p-5"><summary className="cursor-pointer font-bold">{item.question}</summary><p className="mx-0 mt-3 text-slate-600">{item.answer}</p></details>)}</div></section><section className="rounded-3xl bg-[#071225] p-7 text-white"><p className="text-sm text-[#FFC46B]">Related service</p><h2 className="mt-2 text-2xl font-black text-white">Turn the outline into an operating system.</h2><Link href={insight.relatedService.href} className="mt-6 inline-flex rounded-full bg-[#FF9312] px-5 py-3 font-semibold text-slate-950">Explore {insight.relatedService.label}</Link></section></div></div></article></main>;
}
