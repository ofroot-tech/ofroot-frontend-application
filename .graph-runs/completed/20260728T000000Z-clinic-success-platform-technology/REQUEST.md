# Request

Implement the OfRoot Technology Phase 2 Clinic Success Platform foundation: clinic-scoped model/design, referral lifecycle, opaque signed referral URL, authenticated event receiver, and aggregate-only dashboard groundwork. Preserve the Health boundary; do not query Health or receive health details. Do not commit, push, deploy, migrate, or change production. Stop at a missing backend/database target gate.
